# Verify skill: tvlog

## What this app is

**tvlog** — a personal TV log on atproto (AT Protocol). TV-only: you search and log TV shows; there is no movie search or movie logging in the UI. Logging-first: the signed-in home leads with the shows you're currently watching, each with its next unwatched episode ready to log in one tap; free-form tags matter more than ratings. Users authenticate with their atproto identity; records are written to their own PDS. (The domain is still log.sims.computer, but the app calls itself "tvlog" everywhere user-visible.)

## Authenticating (do this first for any logged-in surface)

Navigate to `/api/auth/test-login`. If test credentials are configured in this environment (they are in preview), this establishes a session as a dedicated test account and redirects to `/`. Confirm success: a **sticky app header** appears across the top of the page — the **"tvlog" wordmark** on the far left, and a single **round profile icon** on the far right (there is **no "Watchlist" pill** — Watchlist lives inside the profile menu). At a phone viewport a **round search button (FAB) also appears fixed at the bottom-right** (it is not rendered at desktop widths, where the header carries a search field instead, and it is never rendered on `/search` or `/settings`). **Logged out there is no header at all** — the header appearing is itself the signal that the session took, and its absence while logged out is correct rather than a missing sign-in affordance.

- If `/api/auth/test-login` returns 404, test credentials are absent — verify only logged-out surfaces and note the limitation.
- **Never attempt the OAuth sign-in path** (`/login` → the primary handle form). It redirects off-origin to a PDS and requires human consent; off-origin traffic is invisible to you and the flow cannot complete. Checking that `/login` renders the form is all that's possible.
- The "app password" disclosure on `/login` posts same-origin and would work, but you don't have credential values — use `/api/auth/test-login` instead.
- Writing data while authenticated is safe and expected: everything lands in the throwaway test account's PDS, not a real user's.

## Surfaces

- `/` — **logged out**: the landing page — the "tvlog" wordmark, the tagline "a personal TV log", and a prominent "Sign in" button linking to `/login`. That centred button is the **only** sign-in affordance: **no app header renders at all when logged out** (no "Sign in" link, no profile icon, no search field), which is deliberate — the logged-out prompts on `/search`, `/watchlist`, and `/settings` carry their own Sign in links. There is **no search access when logged out** (search uses a personal TMDB key). **Signed in**: a **watching-first home**, not the full diary.
  - The page itself has **no heading of its own**: there is **no `h1` reading "Watching"** and no in-page "Watchlist" pill — the wordmark and the profile icon live in the shared header above (Watchlist is an item inside that profile menu), and the card grid starts directly beneath it. An absent "Watching" heading is correct, not a missing section.
  - It leads with the watching grid — a grid of cards for the shows currently in progress that **have a next unwatched episode ready to log**, most recent activity first, capped at a sensible number (~12). Each card is a **poster** (loaded same-origin through `/_next/image?...`) with a round white **checkmark log button overlaid on the poster itself, at its bottom-right corner**, followed by a two-line body: line 1 is the **show title in "Show Title · S<n>E<m>" form** — the title (a link to `/show/<tmdbId>`) with a dim ` · S<n>E<m>` suffix, the whole line truncating to one line — and line 2 is the **next episode's title** (also truncated to one line; an episode with no name on TMDB falls back to "Episode <m>"). There is **no bottom row** under the body and **no standalone "S<n>E<m>" label** or "Next · S<n>E<m>" line — the season/episode reads only as that suffix on the title line, and the log button is on the artwork, not in the body. Tapping the button must **not** navigate to the show page (it sits above the poster's link, not inside it), while tapping the poster or the title does. The checkmark button has **no visible text**; its accessible name is **"Log S<n>E<m>"** (e.g. `aria-label="Log S2E3"`). Tapping it opens the **log sheet preloaded with that exact next episode** (the same no-picker sheet described below, with "Just Finished" preselected) — one tap on the sheet's "Log watch" submits. After a successful log, the home re-renders and **that card advances to the following episode**; if that was the finale (the show becomes caught up), the card **drops out of the grid** on refresh.
  - **Caught-up shows are not shown here at all.** A show with every aired episode already watched has nothing to log next, so it is filtered out of the Watching grid entirely (it is not hidden data — it still shows on its own show page, and the watch records are still in the PDS). There is no per-card "caught up" state.
  - **The watching grid is the whole page.** There is **no "Recent" section** and no diary on the home — no `h2` reading "Recent", no day-grouped entry list below the grid. Its absence is correct, not a missing section or a failed fetch: the diary was removed from this surface (it still exists behind `GET /api/diary`). Below the grid there is simply nothing.
  - **Empty states** (either replaces the card grid; nothing else renders below it): if the account has **no shows in progress at all**, a panel reads **"Nothing in progress yet."**; if it has in-progress shows but **every one is caught up**, the panel instead reads **"You're all caught up."**. Both offer a **"Search shows"** link pointing at `/search` — no error, no blank section. The **"Nothing in progress yet."** variant (and only that one) also shows a secondary **"Import your Trakt history"** link pointing at `/settings`; the "You're all caught up." variant does not. (The test account normally shows cards, not either empty state, so this CTA is verifiable by code inspection rather than in the browser.)
- `/install` — the **install walkthrough**, reachable **signed in or out** (no auth, no redirect). A history-aware **"← Back"** control at the top, an `h1` reading **"Install tvlog"**, a one-line why ("Add it to your home screen: full-screen, one tap from your phone, and it stays signed in."), then an ordered list of **5 numbered steps** for adding the site to an iOS home screen: (1) open tvlog.sims.computer in Safari and tap ···, (2) tap Share, (3) tap "Add to Home Screen", (4) tap Add, (5) open it from the home screen and sign in. Steps 1–3 each show a **real iPhone screenshot** served same-origin from `/install/step-1-safari-menu.png`, `/install/step-2-share.png`, `/install/step-3-add-to-home-screen.png` (via `/_next/image?...` or directly — all three must return `200`); steps 4 and 5 use inline illustrations with `role="img"` and an aria-label (step 4 embeds the app icon `/icons/icon-192.png`). Below the steps is a small **"On Android"** panel (Chrome's Install button in the banner, or ⋮ → Add to Home screen). No network calls beyond same-origin assets/icons; **renders at a phone viewport (390px) with no horizontal overflow**, and centred with a modest max width on desktop. The install banner (below) is **never shown on this page**.
- **Header (signed in)** — one sticky bar at the top of every page, ~3.5rem tall, dark, with a thin bottom border; its contents sit in the same `max-w-3xl` column the pages use. It scrolls with the document only until it reaches the top edge, then stays put. Left to right:
  - **Left slot** — the **"tvlog" wordmark** (a bold link to `/`) on most pages. On `/show/<id>` the wordmark is replaced by a history-aware back control reading **"← Back"** (see below). Only one of the two is ever present. **On `/search` the left slot is empty** — neither wordmark nor back control renders there (the search field starts at the left edge instead).
  - **Search field** — an input with the accessible name **"Search"** and placeholder **"Search shows"**. At **desktop widths it is present on every signed-in page except `/settings`**, compact and right-aligned before the profile icon; at **phone widths it is present only on `/search`**, where it fills the row to the left of the × Close button. **`/settings` never shows it, at any width** — that absence is correct, not a missing control. Typing in it is how you search from anywhere: after ~300ms of quiet the app navigates to `/search?q=<query>` (from `/search` itself it rewrites `?q=` in place rather than stacking history entries), and the results render on the page below. Pressing Enter does the same immediately.
  - **Right cluster** — a single round **profile icon** (aria-label "Account menu"), and nothing else. There is **no "Watchlist" pill in the header** — that pill was removed; its absence is correct rather than a missing control. Clicking the icon opens a small popover menu containing, top to bottom, a **"Watchlist"** item (a link to `/watchlist`), a **"Settings"** item (a link to `/settings`), and a **"Log out"** button. There is no visible handle/username and no "Log a watch" button anywhere.
  - The right cluster is **hard right-aligned at every viewport width**, including a 390px phone: the wordmark sits at the far left edge of the bar and the profile icon at the far right, with the (possibly hidden) search area taking up all the space between them. A profile icon bunched up next to the wordmark is a bug.
  - **On `/search` the right cluster is a round × "Close search" button instead** — no profile icon there (see `/search` below).
  - **Logged out the header is absent entirely** on every surface, including `/login` and the logged-out prompts.
- **Search FAB (signed in)** — a round floating action button fixed at the **bottom-right** (aria-label "Search") with a magnifying-glass icon. It is **phone-only** — at desktop widths it is not rendered at all, because the header search field is always there instead — and it is **not rendered on `/search`** (you are already there, and the header input has the focus) **or on `/settings`** (that page has no search affordance at all). Tapping it navigates to `/search`; it is a button rather than a link so it can hand the keyboard to the search field on iOS. Its absence at a desktop viewport, or on `/search` or `/settings`, is correct rather than a missing control.
- `/search` — **signed in only**. If visited while logged out, it shows a "Sign in to search" prompt with a Sign in link rather than a search box. When signed in: a TMDB-backed **TV-only** search, and the page is **nothing but its results** — the search input lives in the header row, and the page has **no `h1` reading "Search"**, no back link of any kind, and **no "Start typing…" hint**. With an empty query the area below the header is simply **blank**; that emptiness is the intended empty state, not a failed render.
  - The **header on `/search` is the search row and nothing else**: the input fills the bar from the left edge, and to its right sits a single round **× button whose accessible name is "Close search"**. There is **no "tvlog" wordmark, no "← Back" control, and no profile icon** on this page — all three are deliberately absent here, not missing. The × is history-aware: it returns to whatever page the search started from (the home, a show page, the watchlist), falling back to `/` on a direct load.
  - The query lives in the URL as **`?q=<query>`**: typing in the header input rewrites it (~300ms debounce, in place), and loading `/search?q=breaking` directly renders those results with the input pre-filled. The input is **focused on arrival**, so typing works immediately without clicking it first. (On iOS Safari the on-screen keyboard should come up too when arriving via the FAB; if it doesn't on a given iOS build, the field is still focused and one tap opens it.)
  - The **FAB is not rendered on this page** — the header input is the search box.
  - After typing, wait for the grid to update before judging results (the ~300ms debounce plus the fetch). Posters must load through `/_next/image?...` on this origin — a visible poster implies the same-origin rule held. Nonsense queries show a no-results state, not an error. There is **no movie/TV filter toggle** (results are always TV shows). Result cards have **no "Log" button**: the whole card is a link — tapping a result navigates to that show's page at `/show/<tmdbId>`.
- `/show/<tmdbId>` — **the show page** (signed in; reachable from a search result tap, a Watching card, or a watchlist poster). Server-rendered for a TV show. The **back control is the header's left slot** — a small **history-aware "← Back"** in place of the wordmark (it returns to wherever you came from — the home, search, or the watchlist — via browser history, falling back to home on a direct load; it is **not** labelled "Search"). There is **no back link inside the page body**; the page opens straight into backdrop/poster, title, year, and overview, followed by a **"Seasons"** list. Season 0 / specials, if present, sort to the end. Each season is a button that **expands to reveal its episodes** (episode number + name), fetched same-origin from `/api/tmdb/season?show=<id>&season=<n>` — the browser must never call TMDB directly here either. Tapping an episode's log control opens the **log sheet** with the show, season, and episode **already chosen from context**: the sheet has **no season/episode picker** — it shows what's being logged as static text (title, then `S<n>E<m> · <episode name>`). Submitting writes records to the test account's PDS — logging during verification is safe and expected. Guard: a season with no episodes shows an empty state, not an error; season data loads must not 404.
  - **Watched indicators** (signed in): the page reads the account's watched episodes for this show off its listItem, so both season headers and episode rows show progress.
    - **Season headers** carry a watched marker on the right, just before the chevron, alongside the existing "<n> episodes" text: a season with **some** episodes watched reads **"<n> of <m> watched"** (e.g. "12 of 26 watched"); a season with **every** episode watched shows a small **green check followed by "Watched"**; a season with **none** watched shows nothing extra — that absence is correct, not a missing indicator. The header stays a **single button** that still expands the season. (A season TMDB reports as having 0 episodes shows neither the episode count nor a marker.)
    - **Episode rows**: an unwatched episode keeps the compact white **"Log"** button, whose accessible name is **"Log S<n>E<m>"** (e.g. `aria-label="Log S4E13"`). A **watched** episode instead shows a green **"Watched"** pill (with a check icon) in that same slot and dims the episode name slightly. The pill is **still a button and still opens the log sheet** — it is how you record a rewatch (tick "Rewatch" in the sheet's "Add tags or a note" disclosure) — and its accessible name is **"Log again S<n>E<m>"**. The two states are told apart by those accessible names: "Log S4E13" vs "Log again S4E13".
    - **Logging from this page flips the row in place**: after the sheet reports success, the row that was just logged switches from the "Log" button to the green "Watched" pill and the season header's count goes up, without a manual reload (the page also re-fetches in the background, so a "Catch up" backfill's earlier episodes turn green too).
    - The test account has **watched many shows**, so a well-known show usually shows fully-watched seasons, a partially-watched one, and untouched ones all on the same page — both row states are observable side by side. A show the account has **never** added to any list correctly shows **no** watched markers at all (and the "Add to Watchlist" button), with no error.
  - **Watchlist button** (signed in): near the title the page shows a watchlist control whose state depends on where the show already sits. A show on **neither** list shows an **"Add to Watchlist"** button; clicking it adds the show and the button flips in place to **"On Watchlist"** (clicking again removes it, back to "Add to Watchlist") — no reload needed. A show **already being watched or fully watched** shows a plain non-interactive **"Watching"** / **"Watched"** badge instead, and **no** add/remove button (correct, not a failure). The test account has watched many shows, so expect the badge on those; pick a show it has never logged to see the "Add to Watchlist" button.
- `/watchlist` — **signed in only**; the destination of the profile menu's **"Watchlist"** item (there is no header pill any more). If visited while logged out, it shows a **"Sign in for your watchlist"** prompt with a Sign in link rather than a grid. When signed in: an `h1` reading **"Watchlist"** (with **no "← tvlog" back link** — the header wordmark goes home) above a **poster grid** of the shows on the watchlist (those in the account's `tv_show_watchlist` list). Each poster links to that show's page at `/show/<tmdbId>` and carries a small round **remove** button (aria-label "Remove … from watchlist") at its top-right corner that drops the show from the grid in place. With an empty watchlist it shows a **"Nothing on your watchlist yet."** panel with a **"Search shows"** link to `/search`, not a blank page or error.
- `/settings` — **signed in only**; reached from the profile popover's "Settings" item. Its header is **wordmark + profile icon only — no search input at any width, and no search FAB at a phone viewport**; both absences are correct rather than missing controls. If visited while logged out, it shows a **"Sign in for settings"** prompt with a Sign in link rather than the page. When signed in: an `h1` reading **"Settings"** (with **no "← tvlog"** back link — the header wordmark goes home), then two sections:
  - **Account** (an `h2` reading "Account") — the signed-in **handle** and **DID**, plus a **"Sign out"** button.
  - **Import from Trakt** (an `h2` reading "Import from Trakt") — the web import flow. There is a file picker labelled **"Choose export .zip"** (a styled `<label>` wrapping a hidden `<input type="file" accept=".zip">`) and an **"Import history"** button (disabled until a file is chosen) for real human uploads. **In preview only** (same env gate as `/api/auth/test-login`), a second button labelled **"Load sample export"** also appears — it runs the import against the committed fixture server-side, so the flow is drivable without filling a file input. Copy explains the import runs in the background and is safe to re-upload (already-imported history is skipped).
- **Import flow** (on `/settings`, signed in): the collector cannot fill file inputs, so drive this with the **"Load sample export"** button (present in preview) rather than the file picker — it runs the same import against the committed fixture `test/fixtures/trakt-tiny.zip` and produces the identical summary/progress/completion UI. Expected sequence, all in-page (no navigation):
  1. A **summary** line appears reading like "Found **10** watch events across **3** shows · **N** already imported · **M** to import · **K** watchlist." (exact numbers depend on prior test-account state; on a first import M=10, K=2, on a repeat M=0, K=0 — either is correct).
  2. While importing, a **progress bar** plus a **phase line** ("Preparing shows… x/3", "Importing… x/10", or "Adding watchlist… x/2"; under rate limiting it reads "Waiting out rate limit until HH:MM"). The fixture is tiny, so this completes in a second or two.
  3. On completion, a green **"Import complete — N watches … added."** line with a note that re-uploading is safe (already-imported history is skipped) and a **"Back to home"** link. Counts are visible throughout. Clicking "Load sample export" again exercises the idempotent path (summary shows 0 to import). Writing to the test account's PDS here is safe and expected.
  - **Single-flight**: while an import is running, a second attempt is rejected — the POST to `/api/import` returns **409** and the page surfaces **"An import is already running for this account."** With the tiny fixture the run finishes almost immediately, so this is best confirmed by code inspection (the route guards on an in-memory per-session job) rather than by racing two imports in the browser.
  - `POST /api/import` returns a summary then runs the import in-process in the background (a multipart `file` field for real uploads, or a JSON `{sample:true}` body for the preview "Load sample export" button, re-gated server-side); `GET /api/import/status` reports phase, counts, and rate-limit waiting state and is polled by the page.
- **Watchlist → watching transition**: logging the first episode of a show that's on the watchlist moves it off the watchlist and into the Watching set. After adding a never-watched show to the watchlist and then logging one of its episodes (from its show page), that show leaves `/watchlist` and appears on the home watching grid — it is never in both at once, and no duplicate card appears.
- **The log sheet** — on a phone viewport it presents as a **bottom sheet** that slides up from the bottom edge (full-width, rounded top corners, a small drag-handle bar at the top, safe-area aware); on a desktop-width viewport it is a centered dialog instead. Backdrop tap, the top-right **× "Close"** button, and the Escape key all dismiss it. On a phone viewport the sheet also **swipes down to dismiss**: a downward drag starting on the header area (the drag handle or the title block), or anywhere in the sheet while its content is already scrolled to the top, drags the panel with the pointer; releasing past roughly 90px — or on a quick downward flick — closes it, and a shorter drag springs the panel back to place. The drag is phone-only (the desktop centered dialog never moves), and it never hijacks the sheet's own scrolling, the wheel picker's columns, or focusing the tag/note fields. Reproducing this needs a real touch/pointer drag, so if the collector cannot synthesise one, verifying the other three dismiss paths is enough — a failure to drag is not a failure of this behavior. The sheet leads with a two-option segmented control (role `radiogroup`, aria-label "When did you watch this?") of **"Just Finished" | "Other"**. **"Just Finished" is preselected** and is the fastest path — it logs the current moment, so one tap on the prominent full-width **"Log watch"** button at the bottom submits immediately with no further input. Choosing **"Other"** reveals a **wheel date picker** (see below). Tags, a note, and a rewatch checkbox are secondary, tucked behind an **"Add tags or a note"** disclosure that expands when tapped. There is **no `<input type=date>`** and **no calendar grid** anywhere.
- **"Catch up" toggle** — between the when-picker area and the "Add tags or a note" disclosure, a log sheet for an episode that **isn't the show's first** (i.e. season > 1 or episode > 1) shows a checkbox row labelled **"Catch up"** (the `<input type="checkbox">` has accessible name **"Catch up"**) over dim helper text reading **"Also log every earlier episode you haven't watched"**. It is **absent when logging S1E1** — that absence is correct, not a failure.
  - **Checking it counts the backfill.** Ticking the box fires exactly **one** same-origin `POST /api/log/catch-up` with `dryRun: true` in the body (no writes happen on a dry run). While it's in flight the helper text reads **"Counting…"**; when it lands the helper text is replaced by either **"Also logs N earlier episodes (S1E1 – S3E4)"** — the count plus the first and last episode of the range — or, when there is nothing to backfill, **"You're already caught up before S<n>E<m>"**. Unchecking the box restores the original helper text and makes no request.
  - **A count of 0 is a correct outcome, not a failure.** The test account has a lot of history, so a show whose earlier episodes are all already logged legitimately yields "You're already caught up before S<n>E<m>". Pick a show/episode deep in a season the account hasn't fully logged if you want to see a non-zero count. Episodes that haven't aired yet are never counted.
  - If the count request fails the helper text reads **"Couldn't count earlier episodes"** and the sheet still submits normally.
  - **Submitting with it ticked** logs the target episode and then writes every counted earlier episode to the test account's PDS (safe and expected — it is a throwaway account). This is a second `POST /api/log/catch-up`, without `dryRun`, sent **after** the `POST /api/log` for the target succeeds. The success panel then reads **"Logged <title>."** plus a second line **"Also logged N earlier episodes."** when N > 0; with N = 0 only the "Logged <title>." line appears. Each backfilled episode is recorded one second before the next so the diary keeps them in episode order, ahead of the target watch.
  - If the backfill fails after the target logged fine, the panel still shows the **success** state plus an amber warning **"Logged S<n>E<m>, but catching up failed: <message>"** — the primary log is never reported as failed. A backfill is capped at 600 episodes; beyond that the route answers 400 with an explanatory message.
  - After a catch-up from a **Watching card**, the home refreshes and that card's next episode reflects the whole backfill (the card advances past the episode just logged, not back to an earlier one).
- **The wheel date picker** (only visible after tapping "Other") — three side-by-side scrollable columns, **Month** (e.g. "Aug"), **Day** ("12"), **Year** ("2026"), iOS-rotary style: each column (role `listbox`, aria-labels "Month" / "Day" / "Year") is a scroll-snap list whose selected value sits in a highlighted center band, with values above/below dimmed and faded at the edges. A value can be selected either by **scrolling the column** (touch, mouse-wheel, or drag) **or by clicking a visible value** (it scrolls to center). Above the columns a live line shows the resolved date **with weekday**, formatted like **"Wednesday, August 12, 2026"**, updating as the selection changes. The day column clamps to the month's length (switching to a short month snaps an out-of-range day down). It defaults to today; the year range runs 2000..current year. Scrolling must not spam network requests or the console — the verify bar expects **zero console errors** and no failed requests.
- **"Use air date" shortcut** — directly under the wheel columns (so, also only after tapping "Other"), an episode whose TMDB air date is known **and already in the past** gets a small ghost button reading **"Aired <Mon D, YYYY> · use this date"** — e.g. "Aired Aug 12, 2026 · use this date" — with the accessible name **"Use air date"**. Clicking it jumps the picker to that date: all three columns **visibly scroll to the aired month/day/year** and the weekday line above updates to match, after which "Log watch" records the watch on that date. The button is **absent** when the episode has no air date or has not aired yet — that absence is correct, not a failure. It is reachable from any log sheet opened with episode context (a Watching card's poster checkmark, or an episode row's "Log" / "Watched" control on a show page).
- **Tag chips link to `/tags/<tag>`, which is not implemented yet** — it 404s by design in this slice. Don't click tag chips while judging console cleanliness, and don't report the 404 as a failure.
- `GET /api/auth/session` — always 200: `{authenticated: true, did, handle}` or `{authenticated: false}`. (Deliberately not a 401 — a 401 on this probe pollutes the browser console on every logged-out page load.) `POST /api/auth/logout` clears the session.
- `/api/healthz` — liveness, `{"ok": true}`. `/api/diary`, `/api/log`, `/api/log/catch-up`, `/api/tmdb/title` back the diary and logging flows (`/api/log/catch-up` both counts a backfill with `dryRun: true` and writes it without). `/api/tmdb/search` returns TV results only; `/api/tmdb/season?show=<id>&season=<n>` returns a season's episodes for the show page.

## Navigation & loading

Every signed-in page is server-rendered on demand against the PDS and TMDB, so **route changes paint a skeleton first and fill in when the data lands**. Clicking the wordmark, a profile-menu item, a Watching card, or a search result switches the page **immediately** to a dark placeholder layout — pulsing `#141414`/`#1c1c1c` blocks in the shape of the real content (a poster grid on `/` and `/watchlist`, a backdrop band plus poster/title plus three season rows on `/show/<id>`, two panels on `/settings`) — which is then replaced by the real content a moment later. The header stays put throughout; it lives in the layout and never re-renders.

- **A skeleton is not an error and not an empty state.** Before judging any signed-in page, wait for real content (a card with a poster and a title, or the page's `h1`) and screenshot that. Judging a page while its pulsing blocks are on screen will read as "no content" when the page is in fact fine.
- The skeletons are `loading.tsx` files — `src/app/loading.tsx`, `src/app/watchlist/loading.tsx`, `src/app/show/[tmdbId]/loading.tsx`, `src/app/settings/loading.tsx`. `/watchlist` and `/settings` render their real `h1` ("Watchlist" / "Settings") in the skeleton too, so the heading can appear a beat before the grid or the panels do.
- Hovering or scrolling links prefetches only the skeleton (not the page's data), so a `?_rsc=` request on hover is expected and cheap. In-page refreshes after a write — logging a watch from a Watching card or an episode row — do **not** swap in a skeleton: the current content stays on screen while the new data is fetched.

## App-specific gotchas

- All API traffic is same-origin by design (TMDB is proxied server-side; the API key must never appear in the browser). If the browser calls api.themoviedb.org or image.tmdb.org directly, that is itself a failure worth reporting. Posters are still optimized through `/_next/image?...` on this origin, but their upstream source varies: search and show-page posters come from image.tmdb.org, while Watching-card posters come from a stored blob on cdn.bsky.app — both are proxied, so the `/_next/image` request is same-origin either way.
- The favicon is `/icon.svg` (linked in head); a 404 for `/favicon.ico` in the console would be a regression.
- Target device is a phone: surfaces are verified at a phone viewport. Touch targets (profile icon, × Close search, FAB, buttons) should be comfortably tappable.
- **The header is width-sensitive**, so judge it against the viewport in use: at a phone width the search field is hidden everywhere except `/search` and the FAB is the way into search; at a desktop width (≥640px) the search field is in the header on every page except `/settings`, and the FAB does not exist. Neither is a missing control — check the other half of the pair before reporting one. (`/settings` is the exception at both widths: no field, no FAB.)

## PWA / installability

The app is an installable PWA (the point is to add it to a phone home screen; iOS Safari is the primary target). All of the following are browser-observable:

- **Web app manifest** — `GET /manifest.webmanifest` returns `200` with content-type `application/manifest+json` and JSON containing `name` and `short_name` both `"tvlog"`, `description` `"a personal TV log"`, `start_url` `"/"`, `display` `"standalone"`, `theme_color` and `background_color` both `"#0a0a0a"`, and an `icons` array (192px + 512px, plus a `purpose: "maskable"` 512px). The document `<head>` links it: `<link rel="manifest" href="/manifest.webmanifest">`.
- **Head metadata** — the `<head>` also carries `<meta name="theme-color" content="#0a0a0a">`, an apple-touch-icon link (`<link rel="apple-touch-icon" ... href="/apple-icon.png">`, 180x180), `<meta name="apple-mobile-web-app-title" content="tvlog">`, and both `apple-mobile-web-app-capable`/`mobile-web-app-capable` = `yes`. The favicon is still `/icon.svg` (unchanged).
- **Icons resolve** — each icon URL returns `200` with an `image/*` content-type: `/icons/icon-192.png`, `/icons/icon-512.png`, `/icons/icon-maskable-512.png`, and `/apple-icon.png`. They share the app mark (a dark rounded TV screen with three dots, one lime).
- **Install prompt** — a compact dark banner fixed near the bottom of the screen (`role="region"`, aria-label **"Install tvlog"**) reading **"Install tvlog"** / "Add it to your home screen for one-tap logging.", shown to both logged-in and logged-out visitors on every page **except `/install`** (where it is suppressed). It sits above the search FAB when the FAB is on screen (signed in, phone width, not `/search`) and otherwise sits at the bottom edge, and it carries an **Install** button on Chromium (which fires the platform install dialog; if the prompt can't be shown it navigates to `/install` instead) or, on iOS Safari, a **"Tap Share, then 'Add to Home Screen'"** instruction line ending in **"How →"** in its place — on iOS the banner's text area is a **link to `/install`** (the walkthrough above), so tapping anywhere except the × opens the walkthrough; a **×** ("Not now") dismisses it and records that in `localStorage` under `tvlog.installPromptDismissed`, so it stays gone on later loads. **It will normally NOT appear to the collector, and that is the correct outcome, not a failure**: headless Chromium does not fire `beforeinstallprompt`, and the collector's UA is not iOS, so neither branch activates and the component renders nothing at all. It is also suppressed whenever the app is already running standalone. Judge it by its absence being silent — no console noise, no network requests, nothing overlapping the FAB or the log sheet (it sits below the sheet's z-layer).
- **Service worker + offline floor** — in a production build, `/sw.js` registers (scope `/`) and becomes the page's controller. It is intentionally minimal: it precaches only a static offline page (`/offline.html`, which reads "tvlog is offline") and serves it **only** when a top-level navigation fails on the network. It caches no app content and **never intercepts API, auth, or asset requests** — so signing in, searching, opening a show, and logging a watch must all behave exactly as without the SW (no stale data, no failed requests, no extra console noise). Registration is production-only, so it will not appear under `next dev`.

## For criteria authors (humans)

Phrase acceptance criteria as browser-observable behavior. Shell-command criteria ("npm run typecheck passes") are not analyzable by the browser collector and come back as errors.
